# 项目技术债清单（暂停自动化测试原因记录）

## Playwright 登录/环境
- 本地 dev 服务尚未稳定启动，E2E 访问 `/questions` 时无法看到“题库管理”标题。
- Playwright 登录脚本依赖教师/学生账号，但生成的 `.auth` 存档失效概率高；需确保 dev 环境注入正确的 Supabase URL/Anon Key，并在测试前手动启动 `npm run dev` 和 `npx supabase start`。
- 如果继续测试，需在用例中再次确认登录态，或直接在每个用例内执行登录流程。

## Supabase 数据 & 列名 ✅ 已解决
- ✅ questions 表新增 `analysis_content` 作为标准解析字段，同时保留 `analysis` 做向后兼容
- ✅ 应用迁移 `20241118000002_update_questions_and_export_tasks.sql`，补齐 province/year/source/image_url/image_key/is_public/tags 等列
- ✅ 创建 `sync_question_analysis` 触发器，确保两个解析列互相同步并刷新 `updated_at`
- ✅ 新增 `export_tasks` 表结构、索引和 RLS，支持导出任务状态查询
- ✅ 运行 `npx supabase db reset`（或 `supabase start` 自动执行迁移）可获得完整 schema
- 解决时间：2025-11-17

## 题篮清空 ✅ 已解决
- ✅ 将 Drawer 内“清空”按钮改为受控 `<Modal>`，点击确认后显式调用 `clearBasket` 并关闭弹窗
- ✅ 移除测试代码中的 storage 事件 workaround，Zustand 状态可自动刷新 UI
- ✅ 为导出任务卡片补充进度、错误提示，方便调试
- 解决时间：2025-11-17
- **原因分析**: 旧的 `Modal.confirm` 行为在测试环境无法等待 `localStorage` 刷新，导致清空后 UI 不更新

## 测试账号
- 使用本地 Supabase Service Role 创建了测试账号：`playwright-teacher@test.com`、`playwright-student@test.com`，密码 `Playwright123!`。在 dev 环境启动后，需重新生成 `.auth` 存档或在用例内登录。

## 后续建议
- 启动本地 Supabase + dev 后再恢复 E2E，或改为在用例里直接调用 Supabase API 进行验证，减少对 UI 异步/跳转的依赖。
- 统一 questions 表字段，与前端提交字段一致，避免创建/更新报错。
