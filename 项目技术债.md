# 项目技术债清单（暂停自动化测试原因记录）

## Playwright E2E 测试认证 ✅ 已解决
- ✅ 修复 dev 服务器就绪检查：[auth.setup.ts:17](tests/e2e/auth.setup.ts#L17)
- ✅ 修复 Auth state 过期验证：[auth.setup.ts:43](tests/e2e/auth.setup.ts#L43)
- ✅ 修复登录 URL 验证逻辑（改用 `!url.pathname.includes('/login')`）
- ✅ **核心修复：React 受控输入兼容性** - 使用 `pressSequentially()` 替代 `fill()` 以正确触发 React 的 onChange 事件：[helpers.ts:50](tests/e2e/helpers.ts#L50)
- ✅ 移除测试用例中的冗余 `ensureLoggedIn()` 调用，改为依赖 Playwright 的 `storageState` 机制
- **测试状态**：认证流程完全正常，global setup 成功，测试可以正常执行业务逻辑
- 解决时间：2025-11-19
- **根本原因**：
  1. 登录表单使用 React 受控输入（`value={email}` + `onChange`）
  2. Playwright 的 `fill()` 方法只更新 DOM value，不触发 React 的合成事件
  3. 导致提交时表单状态为空，Supabase 返回 "missing email or phone" 错误
  4. 测试用例错误地调用了 `ensureLoggedIn()`，与 `storageState` 冲突

## Supabase 数据 & 列名 ✅ 已解决
- ✅ questions 表新增 `analysis_content` 作为标准解析字段，同时保留 `analysis` 做向后兼容
- ✅ 应用迁移 `20241118000002_update_questions_and_export_tasks.sql`，补齐 province/year/source/image_url/image_key/is_public/tags 等列
- ✅ 创建 `sync_question_analysis` 触发器，确保两个解析列互相同步并刷新 `updated_at`
- ✅ 新增 `export_tasks` 表结构、索引和 RLS，支持导出任务状态查询
- ✅ 运行 `npx supabase db reset`（或 `supabase start` 自动执行迁移）可获得完整 schema
- 解决时间：2025-11-17

## 题篮清空 ✅ 已解决
- ✅ 将 Drawer 内“清空”按钮改为受控 `<Modal>`，点击确认后显式调用 `clearBasket` 并关闭弹窗
- ✅ 移除测试代码中的 storage 事件 workaround，Zustand 状态可自动刷新 UI
- ✅ 为导出任务卡片补充进度、错误提示，方便调试
- 解决时间：2025-11-17
- **原因分析**: 旧的 `Modal.confirm` 行为在测试环境无法等待 `localStorage` 刷新，导致清空后 UI 不更新

## 测试账号
- 使用本地 Supabase Service Role 创建了测试账号：`playwright-teacher@test.com`、`playwright-student@test.com`，密码 `Playwright123!`。在 dev 环境启动后，需重新生成 `.auth` 存档或在用例内登录。

## Supabase 登录态 ✅ 已解决
- ✅ 修复 `/api/auth/set-session` API，从不可靠的 `headers.append()` 改为使用 Next.js `cookies()` API
- ✅ 设置 3 种格式的 cookie 以确保兼容性：`sb-127-auth-token`、`sb-Rixindemo-codex-m1-auth-token`、`sb-auth-token`
- ✅ 添加 cookie 验证逻辑，确认设置成功
- ✅ 创建测试脚本 `scripts/test-set-session.mjs` 验证 API 正确返回 3 个 Set-Cookie 头
- ✅ 修复 Playwright `auth.setup.ts`，添加 dev 服务器就绪检查、session 过期验证、正确的登录 URL 验证
- ✅ 创建环境检查脚本 `scripts/check-test-env.mjs` 用于 E2E 测试前的预检
- 解决时间：2025-11-19
- **原因分析**：Next.js App Router 中 `headers.append()` 对 Set-Cookie 不可靠，必须使用官方 `cookies()` API

## 教师端作业管理偶发报错 ✅ 已解决
- ✅ 修复 4 个作业管理相关页面的 useEffect 重复调用问题
  - `src/app/assignments/page.tsx` - 作业列表页
  - `src/app/assignments/create/page.tsx` - 发布作业页
  - `src/app/assignments/[id]/page.tsx` - 作业详情页
  - `src/app/assignments/[id]/grade/[submissionId]/page.tsx` - 批改页
- ✅ 使用 `useCallback` 包装所有异步函数，避免函数重复创建导致 useEffect 无限循环
- ✅ 替换所有 `alert()` 为优雅的 UI 错误提示卡片，支持重试和关闭操作
- ✅ 添加详细的 Supabase 错误日志（code, message, details, hint）便于调试
- ✅ 添加 `error` state 统一管理错误状态，避免双重弹窗
- 解决时间：2025-11-19
- **原因分析**：
  1. useEffect 依赖项中的函数每次渲染重新创建，导致重复执行
  2. alert() 阻塞用户体验，且在测试环境表现异常
  3. 错误日志不详细，无法快速定位 Supabase RLS 或查询问题

## 题目创建页面超时 ✅ 已解决
- ✅ 创建 `/api/profile` API 路由用于服务端获取用户信息：[src/app/api/profile/route.ts](src/app/api/profile/route.ts)
- ✅ 修改题目创建页面使用 API 路由而非客户端 Supabase SDK：[src/app/questions/create/page.tsx](src/app/questions/create/page.tsx)
- ✅ **解决环境变量问题**：系统环境变量 `NEXT_PUBLIC_SUPABASE_URL` 覆盖了 `.env.local`，导致连接到云端 Supabase
  - **根本原因**：Windows 系统环境变量优先级高于 `.env.local` 文件
  - **临时方案**：启动服务器时显式设置环境变量 `export NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321`
  - **永久方案**：清除 Windows 系统环境变量中的 `NEXT_PUBLIC_SUPABASE_URL` 和 `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- 解决时间：2025-11-19
- **技术要点**：
  1. API 路由在服务端执行，避免了客户端 Supabase SDK 的网络超时问题
  2. API 路由从 cookie 中读取 session，支持多种 cookie 格式（`sb-auth-token`、`sb-127-auth-token`、`sb-Rixindemo-codex-m1-auth-token`）
  3. 从 JWT access_token 中解码 user_id，确保在 session.user 不存在时也能正常工作
  4. 使用 Authorization header 传递 access_token 给 Supabase 查询 profiles 表
- **相关文件**：
  - [src/app/api/profile/route.ts](src/app/api/profile/route.ts) - 新增 API 路由
  - [src/app/questions/create/page.tsx](src/app/questions/create/page.tsx) - 修改为调用 API
  - [tests/e2e/helpers.ts](tests/e2e/helpers.ts#L139-147) - E2E 测试辅助函数

## 后续建议
- **优先修复题目创建页面超时问题**，建议采用服务端组件或 API 路由方案
- 启动本地 Supabase + dev 后再恢复 E2E，或改为在用例里直接调用 Supabase API 进行验证，减少对 UI 异步/跳转的依赖。
- 统一 questions 表字段，与前端提交字段一致，避免创建/更新报错。
