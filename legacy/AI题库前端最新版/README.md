# Legacy 说明（AI题库前端最新版）

> 最后更新：2025-12-03

此目录保留的是 2024 年旧版“AI 题库”前端代码快照，仅作为设计参考和组件备份，**不会被当前 Next.js App Router 项目直接引用或构建**。在执行任何重命名、删除或迁移前，请确认：

1. `src/` 与 `components/` 中已经存在替代实现；
2. 没有脚本或 CI 步骤使用到该目录；
3. 如需回溯，请在 `docs/project-governance/file-archive-log.md` 中记录操作。

如果你只是想了解当前正式版本，请参阅 `src/` 目录以及 `docs/technical` 下的最新界面/交互文档。
