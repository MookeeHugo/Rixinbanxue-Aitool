# 阶段3 - 前端集成与测试计划

**日期**: 2025-11-23
**前置阶段**: 阶段1（稳定性修复）✅, 阶段2（功能完善）✅
**目标**: 将服务端录制和文件管理功能集成到前端 UI，完成端到端测试

---

## 📋 完成情况

### 已完成的后端基础设施 ✅

**阶段1**: WebRTC 连接优化、错误处理框架、日志系统
**阶段2**:
- 服务端录制（LiveKit Egress）
- Webhook 事件处理
- 存储配额管理
- 文件管理 API
- 自动清理任务

### 需要实现的前端功能 🔄

本阶段将创建用户界面，让教师能够使用这些后端功能。

---

## 🎯 任务清单

### 任务 3.1: 录制控制 UI 组件 ⏳

#### 3.1.1 创建 RecordingControls 组件
**位置**: `src/components/live/RecordingControls.tsx`

**功能需求**:
- 开始/停止录制按钮
- 录制状态指示器（recording/idle/error）
- 录制时长显示
- 错误提示

**API 集成**:
```typescript
// 开始录制
POST /api/live-sessions/{sessionId}/start-recording

// 停止录制
POST /api/live-sessions/{sessionId}/stop-recording

// 获取录制状态
GET /api/live-sessions/{sessionId}/recordings
```

**UI 设计要点**:
- 录制中显示红色脉冲指示器
- 显示录制时长计时器
- 配额不足时禁用录制按钮
- 停止录制确认对话框

---

#### 3.1.2 集成到 LiveKitRoom 组件
**文件**: `src/components/live/LiveKitRoom.tsx`

**修改内容**:
```typescript
import { RecordingControls } from './RecordingControls';

// 在工具栏中添加录制控制
<div className="live-toolbar">
  <RecordingControls sessionId={sessionId} />
  {/* 其他控制按钮 */}
</div>
```

**权限控制**:
- 仅教师/主持人显示录制按钮
- 学生可查看录制状态（只读）

---

### 任务 3.2: 存储配额显示组件 ⏳

#### 3.2.1 创建 StorageQuotaCard 组件
**位置**: `src/components/storage/StorageQuotaCard.tsx`

**功能需求**:
- 显示已用/总配额
- 进度条可视化
- 剩余空间提示
- 录制文件数量统计

**API 集成**:
```typescript
GET /api/storage/stats
// 返回: { stats: { usagePercentage, formattedTotalSize, formattedQuota, ... } }
```

**UI 设计**:
```tsx
<Card>
  <CardHeader>存储使用情况</CardHeader>
  <CardContent>
    <Progress value={usagePercentage} />
    <p>{formattedTotalSize} / {formattedQuota}</p>
    <p>剩余: {formattedRemaining}</p>
    <p>录制文件: {recordingCount} 个</p>
  </CardContent>
</Card>
```

**状态管理**:
- 使用 SWR 自动刷新（每 30 秒）
- 录制完成后手动刷新

---

#### 3.2.2 集成到设置页面
**位置**: `src/app/settings/page.tsx` 或创建 `src/app/recordings/page.tsx`

**页面结构**:
```tsx
<div className="recordings-page">
  <StorageQuotaCard />
  <RecordingsList />
</div>
```

---

### 任务 3.3: 录制文件列表页面 ⏳

#### 3.3.1 创建 RecordingsList 组件
**位置**: `src/components/recordings/RecordingsList.tsx`

**功能需求**:
- 显示所有录制文件（表格或卡片视图）
- 文件信息: 标题、时长、大小、创建时间、状态
- 操作按钮: 播放、下载、删除
- 分页/无限滚动
- 搜索和筛选（按会话、日期、状态）

**API 集成**:
```typescript
// 获取录制列表
GET /api/live-sessions/{sessionId}/recordings
// 返回: { recordings: [...], metadata: { count, totalSize } }

// 删除录制
DELETE /api/live-sessions/{sessionId}/recordings/{recordingId}
```

**UI 设计**:
```tsx
<Table>
  <thead>
    <tr>
      <th>标题</th>
      <th>时长</th>
      <th>大小</th>
      <th>状态</th>
      <th>创建时间</th>
      <th>操作</th>
    </tr>
  </thead>
  <tbody>
    {recordings.map(rec => (
      <RecordingRow key={rec.id} recording={rec} onDelete={handleDelete} />
    ))}
  </tbody>
</Table>
```

**交互设计**:
- 删除前确认对话框
- 删除成功后 toast 提示
- 删除后自动刷新列表
- 播放录制文件（模态框或新页面）

---

#### 3.3.2 创建 RecordingPlayer 组件
**位置**: `src/components/recordings/RecordingPlayer.tsx`

**功能需求**:
- 播放 MP4 录制文件
- 视频控制条（播放/暂停、进度、音量）
- 全屏支持
- 倍速播放

**实现方式**:
- 使用 HTML5 `<video>` 或 video.js
- 从 Supabase Storage 获取文件 URL

---

### 任务 3.4: 录制状态实时更新 ⏳

#### 3.4.1 实现录制状态轮询
**位置**: `src/hooks/useRecordingStatus.ts`

**功能需求**:
- 轮询获取录制状态（每 5 秒）
- 状态变化时触发回调
- 组件卸载时清理定时器

**实现**:
```typescript
export function useRecordingStatus(sessionId: string) {
  const [status, setStatus] = useState<RecordingStatus | null>(null);

  useEffect(() => {
    const interval = setInterval(async () => {
      const res = await fetch(`/api/live-sessions/${sessionId}/recordings`);
      const data = await res.json();
      setStatus(data.status);
    }, 5000);

    return () => clearInterval(interval);
  }, [sessionId]);

  return status;
}
```

---

#### 3.4.2 Webhook 实时通知（可选）
**位置**: `src/lib/live/recording-notifications.ts`

**功能需求**:
- 使用 Server-Sent Events (SSE) 或 WebSocket
- 录制完成时推送通知到前端
- 显示 toast 提示"录制已完成"

**实现方式**:
```typescript
// API 端点
GET /api/live-sessions/{sessionId}/recording-events

// 前端订阅
const eventSource = new EventSource(`/api/live-sessions/${sessionId}/recording-events`);
eventSource.onmessage = (event) => {
  const data = JSON.parse(event.data);
  if (data.type === 'recording_completed') {
    toast.success('录制已完成');
    refetchRecordings();
  }
};
```

---

### 任务 3.5: E2E 测试 ⏳

#### 3.5.1 录制功能测试
**位置**: `tests/e2e/live-recording.spec.ts`

**测试用例**:
```typescript
test.describe('直播录制功能', () => {
  test('教师应该能够开始和停止录制', async ({ page }) => {
    // 1. 创建直播会话
    await page.goto('/live/new');
    await page.fill('input[name="title"]', '测试录制');
    await page.click('button[type="submit"]');

    // 2. 进入直播间
    await page.waitForURL(/\/live\/.+/);

    // 3. 开始录制
    await page.click('button[aria-label="开始录制"]');
    await expect(page.locator('.recording-indicator')).toBeVisible();

    // 4. 等待 10 秒
    await page.waitForTimeout(10000);

    // 5. 停止录制
    await page.click('button[aria-label="停止录制"]');
    await page.click('button:has-text("确认")'); // 确认对话框

    // 6. 验证录制列表中有新录制
    await page.goto('/recordings');
    await expect(page.locator('table tbody tr').first()).toBeVisible();
  });

  test('学生不应该看到录制按钮', async ({ page }) => {
    // 使用学生账号登录
    await page.goto('/live/session-id');
    await expect(page.locator('button[aria-label="开始录制"]')).toBeHidden();
  });
});
```

---

#### 3.5.2 配额管理测试
**位置**: `tests/e2e/storage-quota.spec.ts`

**测试用例**:
```typescript
test.describe('存储配额管理', () => {
  test('应该显示存储使用情况', async ({ page }) => {
    await page.goto('/recordings');
    await expect(page.locator('.storage-quota-card')).toBeVisible();
    await expect(page.locator('.progress-bar')).toBeVisible();
  });

  test('配额不足时应该禁用录制按钮', async ({ page }) => {
    // Mock API 返回配额已满
    await page.route('/api/storage/stats', route => {
      route.fulfill({
        json: {
          stats: {
            usagePercentage: 100,
            remainingQuota: 0
          }
        }
      });
    });

    await page.goto('/live/session-id');
    await expect(page.locator('button[aria-label="开始录制"]')).toBeDisabled();
  });
});
```

---

#### 3.5.3 文件管理测试
**位置**: `tests/e2e/recording-management.spec.ts`

**测试用例**:
```typescript
test.describe('录制文件管理', () => {
  test('应该能够删除录制文件', async ({ page }) => {
    await page.goto('/recordings');

    const firstRow = page.locator('table tbody tr').first();
    await firstRow.locator('button[aria-label="删除"]').click();

    // 确认删除
    await page.locator('button:has-text("确认删除")').click();

    // 验证成功提示
    await expect(page.locator('.toast-success')).toContainText('录制已删除');
  });

  test('应该能够播放录制文件', async ({ page }) => {
    await page.goto('/recordings');

    const firstRow = page.locator('table tbody tr').first();
    await firstRow.locator('button[aria-label="播放"]').click();

    // 验证视频播放器出现
    await expect(page.locator('video')).toBeVisible();
    await expect(page.locator('video')).toHaveAttribute('src', /.+\.mp4$/);
  });
});
```

---

### 任务 3.6: 数据库迁移和配置 ⏳

#### 3.6.1 运行 Webhook Events 迁移
```bash
# 本地 Supabase
supabase migration up

# 或手动执行
psql $DATABASE_URL -f supabase/migrations/20241123000001_add_webhook_events.sql
```

**验证**:
```sql
-- 检查表是否创建
SELECT * FROM webhook_events LIMIT 1;

-- 测试清理函数
SELECT cleanup_old_webhook_events();
```

---

#### 3.6.2 配置环境变量
**文件**: `.env.local`

**新增配置**:
```bash
# Cron 任务密钥（生产环境）
CRON_SECRET=your_random_secret_key_here_min_32_chars

# LiveKit (确认已有)
LIVEKIT_API_KEY=your_api_key
LIVEKIT_API_SECRET=your_api_secret
LIVEKIT_URL=https://your-livekit-server.com

# Supabase (确认已有)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

**生成 CRON_SECRET**:
```bash
# 使用 OpenSSL 生成随机密钥
openssl rand -base64 32
```

---

#### 3.6.3 配置 LiveKit Webhook
**LiveKit Cloud Dashboard** → **Settings** → **Webhooks**

**配置项**:
- Webhook URL: `https://your-domain.com/api/live-webhooks/livekit`
- Events: 勾选所有事件（room_started, room_finished, egress_ended 等）
- Secret: 使用 `LIVEKIT_API_SECRET`

**测试 Webhook**:
```bash
# 使用 LiveKit 测试工具发送测试事件
curl -X POST https://your-domain.com/api/live-webhooks/livekit \
  -H "Authorization: sha256=..." \
  -H "Content-Type: application/json" \
  -d '{"event":"egress_ended","id":"test_123",...}'
```

---

### 任务 3.7: 用户文档 ⏳

#### 3.7.1 创建用户使用指南
**位置**: `docs/user-guide/recording.md`

**内容大纲**:
1. **录制功能介绍**
   - 什么是服务端录制
   - 录制文件格式和质量
   - 存储配额说明

2. **如何开始录制**
   - 进入直播间
   - 点击录制按钮
   - 停止录制

3. **管理录制文件**
   - 查看录制列表
   - 播放录制
   - 下载录制
   - 删除录制

4. **常见问题**
   - 录制失败怎么办
   - 配额不足怎么办
   - 如何清理旧录制

---

#### 3.7.2 创建管理员文档
**位置**: `docs/admin-guide/recording-management.md`

**内容大纲**:
1. **系统配置**
   - 环境变量设置
   - LiveKit Webhook 配置
   - Vercel Cron 设置

2. **监控和维护**
   - 查看录制任务日志
   - 监控存储使用量
   - 手动触发清理任务

3. **故障排查**
   - Webhook 验证失败
   - 录制文件上传失败
   - 清理任务未执行

4. **配额调整**
   - 修改用户配额
   - 修改全局配额
   - 配额预警设置

---

## 📊 进度追踪

| 任务 | 状态 | 预计工时 | 优先级 |
|------|------|---------|--------|
| 3.1.1 RecordingControls 组件 | ⏳ 待开始 | 4h | P0 |
| 3.1.2 集成到 LiveKitRoom | ⏳ 待开始 | 2h | P0 |
| 3.2.1 StorageQuotaCard 组件 | ⏳ 待开始 | 3h | P1 |
| 3.2.2 集成到设置页面 | ⏳ 待开始 | 1h | P1 |
| 3.3.1 RecordingsList 组件 | ⏳ 待开始 | 6h | P0 |
| 3.3.2 RecordingPlayer 组件 | ⏳ 待开始 | 3h | P1 |
| 3.4.1 录制状态轮询 | ⏳ 待开始 | 2h | P1 |
| 3.4.2 Webhook 实时通知 | ⏳ 待开始 | 4h | P2 (可选) |
| 3.5.1 录制功能 E2E 测试 | ⏳ 待开始 | 4h | P0 |
| 3.5.2 配额管理测试 | ⏳ 待开始 | 2h | P1 |
| 3.5.3 文件管理测试 | ⏳ 待开始 | 3h | P1 |
| 3.6.1 运行数据库迁移 | ⏳ 待开始 | 0.5h | P0 |
| 3.6.2 配置环境变量 | ⏳ 待开始 | 0.5h | P0 |
| 3.6.3 配置 LiveKit Webhook | ⏳ 待开始 | 1h | P0 |
| 3.7.1 用户使用指南 | ⏳ 待开始 | 2h | P1 |
| 3.7.2 管理员文档 | ⏳ 待开始 | 2h | P2 |

**总预计工时**: 约 **39.5 小时**
**核心功能 (P0)**: 约 **19.5 小时**

---

## 🎯 验收标准

### 功能验收
- [ ] 教师可以在直播间开始/停止录制
- [ ] 录制状态实时显示（录制中/已停止）
- [ ] 录制文件自动保存到列表
- [ ] 用户可以查看存储配额使用情况
- [ ] 用户可以播放、下载、删除录制文件
- [ ] 配额不足时无法开始录制
- [ ] 学生不可见录制控制按钮

### 性能验收
- [ ] 录制列表加载时间 < 1秒
- [ ] 视频播放延迟 < 2秒
- [ ] 删除操作响应时间 < 500ms
- [ ] 配额查询响应时间 < 300ms

### 测试验收
- [ ] 所有 E2E 测试通过
- [ ] 测试覆盖率 > 80%
- [ ] 无 TypeScript 编译错误
- [ ] 无 ESLint 警告

### 文档验收
- [ ] 用户使用指南完整清晰
- [ ] 管理员文档包含所有配置步骤
- [ ] API 文档更新（Swagger/OpenAPI）
- [ ] README 包含录制功能介绍

---

## 🚀 实施建议

### 第一周（核心功能）
1. 实现 RecordingControls 组件
2. 实现 RecordingsList 组件
3. 集成到现有页面
4. 运行数据库迁移和配置

### 第二周（增强功能）
1. 实现 StorageQuotaCard 组件
2. 实现 RecordingPlayer 组件
3. 添加录制状态轮询
4. 编写 E2E 测试

### 第三周（测试和文档）
1. 完善 E2E 测试
2. 修复发现的 Bug
3. 编写用户和管理员文档
4. 进行用户验收测试

---

## 🔄 与现有系统的集成点

### LiveKitRoom 组件集成
**文件**: `src/components/live/LiveKitRoom.tsx`

**修改位置**: 工具栏区域（约第 400 行）
```tsx
// 现有工具栏
<div className="flex items-center gap-2">
  {/* 屏幕共享按钮 */}
  <ScreenShareToggle />

  {/* 聊天按钮 */}
  <ChatToggle />

  {/* 新增：录制控制 */}
  {isTeacher && (
    <RecordingControls sessionId={sessionId} />
  )}
</div>
```

### 导航菜单集成
**文件**: `src/components/layout/MainNav.tsx` 或类似文件

**新增菜单项**:
```tsx
<NavItem href="/recordings" icon={VideoIcon}>
  我的录制
</NavItem>
```

### 权限检查集成
**文件**: 录制控制组件内部

**权限逻辑**:
```typescript
const canRecord = useMemo(() => {
  return user?.role === 'teacher' || session?.created_by === user?.id;
}, [user, session]);
```

---

## 📝 注意事项

### 安全考虑
1. **权限验证**: 所有删除操作必须验证用户是创建者
2. **签名验证**: Webhook 必须验证 LiveKit 签名
3. **配额限制**: 严格执行存储配额，防止滥用

### 用户体验
1. **加载状态**: 所有异步操作显示 loading 指示器
2. **错误处理**: 友好的错误提示，避免技术术语
3. **确认对话框**: 删除等危险操作必须二次确认

### 性能优化
1. **列表分页**: 录制列表应分页加载，避免一次性加载大量数据
2. **缓存策略**: 使用 SWR 缓存配额数据，减少 API 调用
3. **懒加载**: 视频播放器按需加载，不要预加载

---

## 🎊 总结

阶段3 将完成录制功能的前端集成，使整个录制系统可供用户使用。完成后，平台将具备:

- ✅ 完整的直播录制功能（客户端 + 服务端）
- ✅ 录制文件管理（列表、播放、删除）
- ✅ 存储配额管理和监控
- ✅ 自动化清理和维护
- ✅ 全面的测试覆盖
- ✅ 完善的用户文档

这将为用户提供一个功能完整、稳定可靠的在线教育直播平台！🚀
