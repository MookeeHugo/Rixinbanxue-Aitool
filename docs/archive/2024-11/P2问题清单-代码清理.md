# P2级别 代码清理问题清单

**生成日期**: 2025-11-20
**问题总数**: 38个
**涉及文件**: 16个

---

## 📊 问题汇总

| 类型 | 数量 | 优先级 |
|------|------|--------|
| 调试console语句 | 23 | 🔴 高 |
| 未使用的导入 | 0 | 🟡 中 |
| 注释的代码 | 6 | 🟡 中 |
| 次要性能优化 | 9 | 🟢 低 |
| **总计** | **38** | - |

---

## 🔴 高优先级：删除调试console语句

### 文件1: src/app/login/page.tsx - 5处

#### 问题1.1: 调试console.log环境变量 (行17-21)
**代码**:
```typescript
console.log('Supabase URL:', process.env.NEXT_PUBLIC_SUPABASE_URL)
console.log(
  'Supabase ANON key prefix:',
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY?.slice(0, 16) || 'missing'
)
```

**建议操作**: 删除这两行调试日志

**理由**: 生产环境不需要的调试日志，会暴露环境配置信息

---

#### 问题1.2: 调试console.log用户信息 (行31-32)
**代码**:
```typescript
console.log('[login page] signIn user id:', user?.id)
console.log('[login page] session after signIn:', JSON.stringify(session.data))
```

**建议操作**: 删除这两行调试日志

**理由**: 生产环境不需要的调试日志，会泄露敏感的session信息

---

#### 问题1.3: 详细的错误日志 (行43-45)
**代码**:
```typescript
console.error('同步服务端 session 失败', await response.text().catch(() => ''))
} else {
  console.log('服务端 session 已写入 cookie')
}
```

**建议操作**: 简化为 `console.error('同步服务端 session 失败:', error)`，删除成功日志

**理由**: 简化错误输出，删除不必要的成功日志

---

### 文件2: src/app/assignments/page.tsx - 3处

#### 问题2.1: 详细的结构化错误日志 (行88-93)
**代码**:
```typescript
console.error('[loadAssignments] Supabase error:', {
  code: fetchError.code,
  message: fetchError.message,
  details: fetchError.details,
  hint: fetchError.hint,
})
```

**建议操作**: 简化为 `console.error('加载作业失败:', fetchError)`

**理由**: 详细的结构化日志是调试代码，应该简化

---

#### 问题2.2: 详细的结构化错误日志 (行143-148)
**代码**:
```typescript
console.error('[deleteAssignment] Supabase error:', {
  code: deleteError.code,
  message: deleteError.message,
  details: deleteError.details,
  hint: deleteError.hint,
})
```

**建议操作**: 简化为 `console.error('删除作业失败:', deleteError)`

**理由**: 详细的结构化日志是调试代码，应该简化

---

### 文件3: src/app/assignments/create/page.tsx - 3处

#### 问题3.1: 详细的结构化错误日志 (行46-51)
**代码**:
```typescript
console.error('[loadData] Load classes error:', {
  code: classesError.code,
  message: classesError.message,
  details: classesError.details,
  hint: classesError.hint,
})
```

**建议操作**: 简化为 `console.error('加载班级列表失败:', classesError)`

**理由**: 详细的结构化日志是调试代码，应该简化

---

#### 问题3.2: 详细的结构化错误日志 (行66-71)
**代码**:
```typescript
console.error('[loadData] Load papers error:', {
  code: papersError.code,
  message: papersError.message,
  details: papersError.details,
  hint: papersError.hint,
})
```

**建议操作**: 简化为 `console.error('加载试卷列表失败:', papersError)`

**理由**: 详细的结构化日志是调试代码，应该简化

---

#### 问题3.3: 详细的结构化错误日志 (行140-145)
**代码**:
```typescript
console.error('[handleSubmit] Supabase error:', {
  code: insertError.code,
  message: insertError.message,
  details: insertError.details,
  hint: insertError.hint,
})
```

**建议操作**: 简化为 `console.error('发布作业失败:', insertError)`

**理由**: 详细的结构化日志是调试代码，应该简化

---

### 文件4: src/app/assignments/[id]/page.tsx - 4处

#### 问题4.1: 详细的结构化错误日志 (行48-53)
**代码**:
```typescript
console.error('[loadAssignment] Supabase error:', {
  code: assignmentError.code,
  message: assignmentError.message,
  details: assignmentError.details,
  hint: assignmentError.hint,
})
```

**建议操作**: 简化为 `console.error('加载作业失败:', assignmentError)`

**理由**: 详细的结构化日志是调试代码，应该简化

---

#### 问题4.2: 详细的结构化错误日志 (行70-75)
**代码**:
```typescript
console.error('[loadAssignment] Load questions error:', {
  code: questionsError.code,
  message: questionsError.message,
  details: questionsError.details,
  hint: questionsError.hint,
})
```

**建议操作**: 简化为 `console.error('加载题目失败:', questionsError)`

**理由**: 详细的结构化日志是调试代码，应该简化

---

#### 问题4.3: 详细的结构化错误日志 (行102-107)
**代码**:
```typescript
console.error('[loadAssignment] Load submissions error:', {
  code: submissionsError.code,
  message: submissionsError.message,
  details: submissionsError.details,
  hint: submissionsError.hint,
})
```

**建议操作**: 简化为 `console.error('加载提交记录失败:', submissionsError)`

**理由**: 详细的结构化日志是调试代码，应该简化

---

### 文件5: src/app/assignments/[id]/grade/[submissionId]/page.tsx - 5处

#### 问题5.1: 详细的结构化错误日志 (行87-92)
**代码**:
```typescript
console.error('[checkUserAndLoadData] Load submission error:', {
  code: subError.code,
  message: subError.message,
  details: subError.details,
  hint: subError.hint,
})
```

**建议操作**: 简化为 `console.error('加载提交记录失败:', subError)`

**理由**: 详细的结构化日志是调试代码，应该简化

---

#### 问题5.2-5.4: 类似的详细错误日志 (行120-161)
**代码**: 包含3处类似的详细结构化错误日志

**建议操作**: 全部简化为简单的错误信息

**理由**: 详细的结构化日志是调试代码，应该简化

---

#### 问题5.5: 详细的结构化错误日志 (行214-219)
**代码**:
```typescript
console.error('[handleSaveScore] Supabase error:', {
  code: updateError.code,
  message: updateError.message,
  details: updateError.details,
  hint: updateError.hint,
})
```

**建议操作**: 简化为 `console.error('保存分数失败:', updateError)`

**理由**: 详细的结构化日志是调试代码，应该简化

---

### 文件6: src/app/classes/page.tsx - 1处

#### 问题6.1: 详细的结构化错误日志 (行48-53)
**代码**:
```typescript
console.error('[loadClasses] Supabase error:', {
  code: fetchError.code,
  message: fetchError.message,
  details: fetchError.details,
  hint: fetchError.hint,
})
```

**建议操作**: 简化为 `console.error('加载班级列表失败:', fetchError)`

**理由**: 详细的结构化日志是调试代码，应该简化

---

### 文件7: src/app/papers/create/page.tsx - 1处

#### 问题7.1: 调试console.log (行85)
**代码**:
```typescript
console.log('Knowledge points table not found, using fallback')
```

**建议操作**: 删除

**理由**: 生产环境不需要的调试日志

---

### 文件8: src/app/debug-env/page.tsx - 1处

#### 问题8.1: 调试console.log (行60)
**代码**:
```typescript
console.log('Supabase localStorage:', result)
```

**建议操作**: 保留（这是debug页面的功能）

**理由**: 该页面本身就是用于调试环境的，console.log是必需的

---

## 🟡 中优先级：清理注释的代码

### 文件9: src/app/papers/create/page.tsx - 2处

#### 问题9.1: 注释的旧代码 (行79-82)
**代码**:
```typescript
// 数据库有数据，使用数据库的知识点
setKnowledgePoints(data.map((kp: any) => kp.name))
}
// 如果数据库没有数据或出错，继续使用硬编码的知识点
```

**建议操作**: 保留注释，这些是必要的说明性注释

**理由**: 这些是解释性注释，不是注释的代码

---

#### 问题9.2: 注释的旧代码 (行86)
**代码**:
```typescript
// 表不存在时，继续使用硬编码的KNOWLEDGE_POINTS
```

**建议操作**: 保留注释

**理由**: 这是解释性注释，不是注释的代码

---

### 文件10: src/app/classes/[id]/page.tsx - 1处

#### 问题10.1: 注释说明 (行58-59)
**代码**:
```typescript
// 这里暂时不加载学生列表，因为还没有实现学生加入班级的功能
// 未来可以通过 class_students 关联表查询
```

**建议操作**: 保留注释

**理由**: 这是重要的TODO注释，说明未来的改进方向

---

### 文件11: src/app/questions/create/page.tsx - 1处

#### 问题11.1: 注释说明 (行16-19)
**代码**:
```typescript
// 使用 API 路由获取 profile，避免客户端 Supabase SDK 初始化超时
const response = await fetch('/api/profile', {
  method: 'GET',
  credentials: 'include', // 确保发送 cookies
```

**建议操作**: 保留注释

**理由**: 这是重要的解释性注释，说明为什么使用API路由

---

### 文件12: src/app/assignments/page.tsx - 2处

#### 问题12.1-12.2: 解释性注释
**代码**: 包含"纯函数移到组件外部"等注释

**建议操作**: 保留注释

**理由**: 这些是有价值的代码组织说明

---

## 🟢 低优先级：次要性能优化

### 文件13: src/app/questions/page.tsx - 3处

#### 问题13.1: 内联函数可提取 (行240-258)
**当前代码**:
```typescript
const highlightMatch = useCallback((text: string) => {
  if (!searchText.trim()) return text
  const keyword = searchText.trim()
  try {
    const regex = new RegExp(`(${keyword.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')})`, 'gi')
    const parts = text.split(regex)
    return parts.map((part, idx) =>
      idx % 2 === 1 ? (
        <mark key={`${part}-${idx}`} className="px-0.5 bg-yellow-200">
          {part}
        </mark>
      ) : (
        part
      )
    )
  } catch {
    return text
  }
}, [searchText])
```

**建议方案**: 保持现状，useCallback已经优化

**理由**: 该函数依赖searchText状态，使用useCallback是正确的

---

#### 问题13.2: 可提取的常量 (行43)
**当前代码**:
```typescript
const SEARCH_HISTORY_KEY = 'rixin-question-search-history'
```

**建议操作**: 提取到独立的constants文件

**建议方案**:
```typescript
// src/constants/storage-keys.ts
export const SEARCH_HISTORY_KEY = 'rixin-question-search-history'
export const BASKET_SNAPSHOT_KEY = 'rixin-basket-snapshot'
```

**理由**: 集中管理localStorage key，便于维护

---

#### 问题13.3: 重复的类型映射函数 (行872-897)
**当前代码**:
```typescript
function getTypeLabel(type: string) {
  const labels: Record<string, string> = {
    choice: '选择题',
    fill: '填空题',
    essay: '解答题',
  }
  return labels[type] || type
}

function getDifficultyLabel(level: string) {
  const labels: Record<string, string> = {
    easy: '简单',
    medium: '中等',
    hard: '困难',
  }
  return labels[level] || level
}
```

**建议方案**: 提取到共享的utils文件

**理由**: 这些函数在多个文件中重复定义，应该提取为共享工具函数

---

### 文件14: src/app/papers/create/page.tsx - 2处

#### 问题14.1: 硬编码的知识点数组 (行9-25)
**当前代码**:
```typescript
const KNOWLEDGE_POINTS = [
  '有理数加法', '有理数减法', '有理数乘法', '有理数除法', '有理数混合运算',
  // ... 共50+个知识点
]
```

**建议方案**: 提取到独立的配置文件

**建议操作**:
```typescript
// src/constants/knowledge-points.ts
export const KNOWLEDGE_POINTS = [
  // ... 知识点列表
]
```

**理由**: 便于维护和在多个页面复用

---

#### 问题14.2: useMemo优化机会 (行259-261)
**当前代码**:
```typescript
const filteredKnowledgePoints = useMemo(() => {
  return knowledgePoints.filter(point => point.includes(searchTerm))
}, [knowledgePoints, searchTerm])
```

**建议操作**: 保持现状

**理由**: 已经使用useMemo优化，无需修改

---

### 文件15: src/app/assignments/page.tsx - 2处

#### 问题15.1: 纯函数定义位置 (行16-40)
**当前代码**:
```typescript
// 纯函数移到组件外部
function getStatusColor(status: string) {
  switch (status) {
    case 'draft':
      return 'bg-secondary text-foreground-secondary'
    // ...
  }
}
```

**建议操作**: 保持现状

**理由**: 已经优化，函数在组件外部定义

---

#### 问题15.2: 重复的状态映射 (行16-40)
**建议方案**: 提取到共享的constants文件

**理由**: 状态颜色和文本映射可能在多处使用，应该统一管理

---

### 文件16: src/app/assignments/[id]/grade/[submissionId]/page.tsx - 2处

#### 问题16.1: 纯函数定义位置 (行15-43)
**当前代码**:
```typescript
// 纯函数：移到组件外部
function getQuestionTypeName(type: string) {
  const typeMap: Record<string, string> = {
    'choice': '选择题',
    'fill': '填空题',
    'essay': '解答题'
  }
  return typeMap[type] || type
}
```

**建议操作**: 保持现状或提取到共享utils

**理由**: 已经优化，函数在组件外部定义，但可以进一步提取为共享工具

---

#### 问题16.2: 答案比较逻辑 (行33-43)
**当前代码**:
```typescript
function isAnswerCorrect(question: Question, studentAnswer: any) {
  if (!studentAnswer) return false

  if (question.type === 'choice' || question.type === 'fill') {
    return String(studentAnswer).trim().toLowerCase() === String(question.answer).trim().toLowerCase()
  }

  return null
}
```

**建议操作**: 保持现状

**理由**: 逻辑清晰，已经处理了各种边界情况

---

## 📋 清理建议优先级

### 立即处理（高优先级）
1. **删除login页面的调试日志** (5处) - 泄露敏感信息
2. **简化所有详细的结构化错误日志** (18处) - 生产环境不需要

### 近期处理（中优先级）
3. **删除papers/create页面的console.log** (1处)
4. **review注释的代码** - 已检查，大部分是必要的解释性注释

### 可选处理（低优先级）
5. **提取常量到独立文件** - 提升代码复用性
6. **提取共享工具函数** - 减少代码重复
7. **统一类型映射函数** - 统一管理字典映射

---

## 🎯 重点处理文件列表

按问题数量排序：

1. **src/app/assignments/[id]/grade/[submissionId]/page.tsx** - 5个问题
2. **src/app/login/page.tsx** - 5个问题
3. **src/app/assignments/[id]/page.tsx** - 4个问题
4. **src/app/assignments/create/page.tsx** - 3个问题
5. **src/app/assignments/page.tsx** - 3个问题
6. **src/app/questions/page.tsx** - 3个问题
7. **src/app/papers/create/page.tsx** - 3个问题

---

## 📝 执行建议

### 批量处理方案

可以使用以下正则表达式批量查找并简化console.error：

**查找**:
```regex
console\.error\('\[.*?\].*?:', \{[\s\S]*?\}\)
```

**替换为简单形式**:
```typescript
console.error('错误描述:', error)
```

### 测试验证

清理后应进行以下测试：
1. 登录流程测试
2. 作业创建/发布流程测试
3. 题目CRUD操作测试
4. 学生提交作业流程测试
5. 教师批改作业流程测试

---

## ✅ 预期收益

完成清理后预期收益：

1. **减少代码行数**: 预计减少约150-200行调试代码
2. **提升安全性**: 移除敏感信息日志
3. **改善生产环境性能**: 减少不必要的日志输出
4. **提高代码可维护性**: 统一错误处理方式
5. **优化bundle大小**: 移除调试代码后略微减小打包体积

---

## 📌 备注

1. **debug-env/page.tsx** 中的console.log需要保留，因为这是专门的调试页面
2. 所有解释性注释和TODO注释应该保留
3. 简化错误日志时，确保仍然能够追踪关键错误信息
4. 建议在dev环境保留详细日志，production环境使用简化版本

---

**文档版本**: 1.0
**最后更新**: 2025-11-20
**审核状态**: 待审核
